review all documentation for completeness
review all URLs in source code and documentation to make sure they still exist
run pylint on codebase and get things clean
Get "nice to have" things running in Cloudbees Jenkins (including coverage, pylint reports, etc.)
start looking at code coverage to see if there are other tests I need to write
rename things (executables, logs, etc.) so v2 and v3 can coexist on the same server
fix documentation to discuss transition between v2 and v3 (what's compatible, what changes)
fix bug in documentation that says Debian package gets stubbed config install
